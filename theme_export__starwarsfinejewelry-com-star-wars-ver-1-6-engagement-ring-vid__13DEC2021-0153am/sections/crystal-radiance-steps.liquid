{% if section.settings.show_section == true %}
<section>
  <div class="page-width">
    {% assign localName = request.locale.name | handle %}
    {% if localName == "english"%}
    {% if section.settings.title != blank %}
    <h2 class="text-center">{{ section.settings.title }}</h2>
    {% endif %}
    {% else %}
    {% if section.settings.title_japanese != blank %}
    <h2 class="text-center">{{ section.settings.title_japanese }}</h2>
    {% endif %}
    {% endif %}
    <div class="grid grid--no-gutters">
      <div class="grid__item medium-up--one-half">
        {% if section.settings.image != blank %}
        <div class="image-wrapper">
          <img class="lazyload"
               src="{{ section.settings.image | img_url: 'master' }}"
               data-src="{{ section.settings.image | img_url:"master" }}"
               data-sizes="auto"
               alt="" />
        </div>
        {% endif %}
      </div>
      <div class="grid__item medium-up--one-half">
        <div class="section-content">
          {% if section.blocks.size > 0 %}
          {% for block in section.blocks %}
          <div class="step">
            {% assign localName = request.locale.name | handle %}
            {% if localName == "english"%}
            <span>Step</span>
            {% else %}
            <span>ステップ</span>
            {% endif %}
            {% assign localName = request.locale.name | handle %}
            {% if localName == "english"%}
            {% if block.settings.step_info != blank %}
            <p>{{ block.settings.step_info }}</p>
            {% endif %}
            {% else %}
            {% if block.settings.step_info_japanese != blank %}
            <p>{{ block.settings.step_info_japanese }}</p>
            {% endif %}
            {% endif %}
          </div>
          {% endfor %}
          {% else %}
          {% include 'no-blocks' %}
          {% endif %}
          {% assign localName = request.locale.name | handle %}
          {% if localName == "english"%}
          {% if section.settings.button_label != blank and section.settings.button_link != blank %}
          <a href="{{ section.settings.button_link }}" class="btn_style2">
            {{ section.settings.button_label }}
          </a>
          {% endif %}
          {% else %}
          {% if section.settings.button_label_japanese != blank and section.settings.button_link != blank %}
          <a href="{{ section.settings.button_link }}" class="btn_style2">
            {{ section.settings.button_label_japanese }}
          </a>
          {% endif %}
          {% endif %}
        </div>
      </div>
    </div>
  </div>
</section>
{% endif %}

{% schema %}
  {
    "name": "Crystal radiance steps",
	"class": "crystal-radiance-steps-section",
    "settings": [
	  {
        "type": "checkbox",
        "id": "show_section",
        "label": "Show Section",
        "default": true
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "Heading"
      },
      {
        "type": "text",
        "id": "title_japanese",
        "label": "Title (Japanese)",
        "default": "カイバー・クリスタル・レイディアンスペンダント夜間使用法"
      },
      {
        "type": "image_picker",
        "id": "image",
        "label": "Image",
        "info": "Recommended size: 633px * 646px"
      },
      {
        "type": "text",
        "id": "button_label",
        "label": "Button label"
      },
      {
        "type": "text",
        "id": "button_label_japanese",
        "label": "Button label (Japanese)"
      },
      {
        "type": "url",
        "id": "button_link",
        "label": "Button link"
      }
    ],
    "blocks":[
      {
        "type": "step",
        "name": "Step",
        "settings": [
          {
            "type": "textarea",
            "id": "step_info",
            "label": "Step"
          },
		  {
            "type": "textarea",
            "id": "step_info_japanese",
            "label": "Step (Japanese)"
          }
        ]
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
